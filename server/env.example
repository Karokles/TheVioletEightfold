# Backend Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to git

# ============================================================================
# REQUIRED VARIABLES (Production)
# ============================================================================

# Server Configuration
NODE_ENV=development
# In production (Render): Set to "production"
PORT=3001
# In production (Render): Auto-set by Render, do not override

# JWT Authentication (REQUIRED in production - server will crash without it)
# Generate with: openssl rand -base64 32
# Minimum length: 32 characters recommended
# Example: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here-min-32-chars

# OpenAI API (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# CORS Configuration (REQUIRED in production)
# Comma-separated list of allowed frontend origins (no trailing slashes)
# Example: http://localhost:5173,https://the-violet-eightfold42.vercel.app
# Must include your Vercel frontend domain in production
# Default dev origins (localhost:3000, localhost:5173) are automatically included
ALLOWED_ORIGINS=https://the-violet-eightfold42.vercel.app,http://localhost:5173

# ============================================================================
# OPTIONAL VARIABLES
# ============================================================================

# Supabase (Optional - feature flag)
# If not set, Supabase features are disabled gracefully
# Get from: https://supabase.com/dashboard → Your Project → Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

